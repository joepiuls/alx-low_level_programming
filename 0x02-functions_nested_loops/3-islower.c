#include "main.h"
/**
* _islower - check if alphabet is lowercase
* return 1 if lowercase is true
* return 0 iflowercase is false
*/
int _islowerw(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
